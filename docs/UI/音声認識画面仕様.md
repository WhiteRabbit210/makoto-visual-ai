# 音声認識画面仕様

## 画面構成

### レイアウト
- **メインビュー + サイドパネル構成**
  - メインエリア: タイムライン表示
  - 右サイドパネル: 話者管理・設定（320px）
  - ボトムパネル: 再生コントロール（80px）

## ヘッダー

### プロジェクトヘッダー
```css
.project-header {
  padding: 20px 24px;
  background: var(--bg-primary);
  border-bottom: 1px solid var(--border-light);
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.project-title {
  font-size: 20px;
  font-weight: 600;
  color: var(--text-primary);
}

.project-status {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 6px 12px;
  background: var(--bg-secondary);
  border-radius: 20px;
  font-size: 13px;
}

.project-status-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: var(--status-success);
  animation: pulse 2s infinite;
}

@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}
```

## タイムライン表示

### タイムラインコンテナ
```css
.timeline-container {
  padding: 24px;
  height: calc(100vh - 180px);
  overflow-y: auto;
  scroll-behavior: smooth;
}

.timeline-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 20px;
}

.timeline-title {
  font-size: 16px;
  font-weight: 500;
  color: var(--text-primary);
}

.timeline-actions {
  display: flex;
  gap: 8px;
}
```

### セグメントアイテム
```css
.segment-item {
  display: flex;
  align-items: flex-start;
  gap: 16px;
  padding: 16px 20px;
  background: var(--bg-card);
  border: 1px solid var(--border-light);
  border-radius: 8px;
  margin-bottom: 12px;
  transition: all 0.2s ease;
  cursor: pointer;
}

.segment-item:hover {
  border-color: var(--border-color);
  box-shadow: var(--shadow-sm);
}

.segment-item--active {
  border-color: var(--primary-blue);
  background: var(--bg-secondary);
}

.segment-item--playing {
  border-color: var(--primary-green);
  background: rgba(82, 196, 26, 0.05);
}

/* タイムスタンプ */
.segment-time {
  min-width: 80px;
  font-size: 13px;
  color: var(--text-muted);
  font-family: var(--font-mono);
}

/* 話者情報 */
.segment-speaker {
  min-width: 120px;
  display: flex;
  align-items: center;
  gap: 8px;
}

.speaker-avatar {
  width: 32px;
  height: 32px;
  border-radius: 50%;
  background: var(--primary-purple);
  color: white;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 14px;
  font-weight: 600;
}

.speaker-name {
  font-size: 14px;
  font-weight: 500;
  color: var(--text-primary);
}

/* 発話内容 */
.segment-content {
  flex: 1;
  font-size: 14px;
  line-height: 1.6;
  color: var(--text-primary);
}

.segment-content--editable {
  padding: 8px 12px;
  background: var(--bg-secondary);
  border: 1px solid var(--border-color);
  border-radius: 6px;
  cursor: text;
}

/* セグメントアクション */
.segment-actions {
  display: flex;
  gap: 4px;
  opacity: 0;
  transition: opacity 0.2s ease;
}

.segment-item:hover .segment-actions {
  opacity: 1;
}
```

### 話者カラー
```css
.speaker-avatar--color-1 { background: #6B2F7B; }
.speaker-avatar--color-2 { background: #4A90E2; }
.speaker-avatar--color-3 { background: #52C41A; }
.speaker-avatar--color-4 { background: #F5A623; }
.speaker-avatar--color-5 { background: #E74C3C; }
.speaker-avatar--color-6 { background: #9B59B6; }
.speaker-avatar--color-7 { background: #1ABC9C; }
.speaker-avatar--color-8 { background: #34495E; }
```

## 話者管理パネル

### パネルヘッダー
```css
.speaker-panel {
  width: 320px;
  height: 100%;
  background: var(--bg-secondary);
  border-left: 1px solid var(--border-light);
  display: flex;
  flex-direction: column;
}

.speaker-panel-header {
  padding: 16px 20px;
  border-bottom: 1px solid var(--border-light);
}

.speaker-panel-title {
  font-size: 16px;
  font-weight: 500;
  color: var(--text-primary);
}
```

### 話者リスト
```css
.speaker-list {
  flex: 1;
  overflow-y: auto;
  padding: 16px;
}

.speaker-item {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 12px;
  background: var(--bg-card);
  border-radius: 8px;
  margin-bottom: 8px;
  transition: all 0.2s ease;
}

.speaker-item:hover {
  box-shadow: var(--shadow-sm);
}

.speaker-item-avatar {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  color: white;
  font-weight: 600;
}

.speaker-item-info {
  flex: 1;
}

.speaker-item-name {
  font-weight: 500;
  color: var(--text-primary);
  margin-bottom: 4px;
}

.speaker-item-stats {
  font-size: 12px;
  color: var(--text-muted);
}

.speaker-item-actions {
  display: flex;
  gap: 4px;
}
```

## 再生コントロール

### プレイヤーバー
```css
.player-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  height: 80px;
  background: var(--bg-primary);
  border-top: 1px solid var(--border-light);
  display: flex;
  align-items: center;
  padding: 0 24px;
  gap: 24px;
}

/* 再生ボタン */
.player-controls {
  display: flex;
  align-items: center;
  gap: 12px;
}

.play-button {
  width: 48px;
  height: 48px;
  border-radius: 50%;
  background: var(--primary-blue);
  color: white;
  border: none;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  transition: all 0.2s ease;
}

.play-button:hover {
  background: #3A7BC8;
  transform: scale(1.05);
}

.play-button:active {
  transform: scale(0.95);
}

.skip-button {
  width: 36px;
  height: 36px;
  border-radius: 50%;
  background: transparent;
  color: var(--text-secondary);
  border: none;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  transition: all 0.2s ease;
}

.skip-button:hover {
  background: var(--bg-hover);
  color: var(--text-primary);
}

/* プログレスバー */
.player-progress {
  flex: 1;
  display: flex;
  align-items: center;
  gap: 16px;
}

.progress-time {
  font-size: 13px;
  color: var(--text-muted);
  font-family: var(--font-mono);
  min-width: 50px;
}

.progress-bar {
  flex: 1;
  height: 6px;
  background: var(--bg-tertiary);
  border-radius: 3px;
  position: relative;
  cursor: pointer;
}

.progress-bar-fill {
  height: 100%;
  background: var(--primary-blue);
  border-radius: 3px;
  position: relative;
}

.progress-bar-thumb {
  position: absolute;
  right: -8px;
  top: 50%;
  transform: translateY(-50%);
  width: 16px;
  height: 16px;
  border-radius: 50%;
  background: var(--primary-blue);
  border: 3px solid white;
  box-shadow: var(--shadow-sm);
  opacity: 0;
  transition: opacity 0.2s ease;
}

.progress-bar:hover .progress-bar-thumb,
.progress-bar--dragging .progress-bar-thumb {
  opacity: 1;
}

/* 音量コントロール */
.volume-control {
  display: flex;
  align-items: center;
  gap: 8px;
}

.volume-icon {
  width: 32px;
  height: 32px;
  border-radius: 6px;
  background: transparent;
  color: var(--text-secondary);
  border: none;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
}

.volume-slider {
  width: 80px;
  height: 4px;
  background: var(--bg-tertiary);
  border-radius: 2px;
  cursor: pointer;
}

.volume-slider-fill {
  height: 100%;
  background: var(--text-secondary);
  border-radius: 2px;
}

/* 再生速度 */
.playback-speed {
  padding: 6px 12px;
  background: var(--bg-secondary);
  border: 1px solid var(--border-light);
  border-radius: 6px;
  font-size: 13px;
  color: var(--text-secondary);
  cursor: pointer;
  transition: all 0.2s ease;
}

.playback-speed:hover {
  background: var(--bg-hover);
  border-color: var(--border-color);
}
```

## 処理状態表示

### プログレス表示
```css
.processing-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.8);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
}

.processing-modal {
  background: var(--bg-card);
  border-radius: 12px;
  padding: 32px;
  min-width: 400px;
  text-align: center;
}

.processing-icon {
  width: 64px;
  height: 64px;
  margin: 0 auto 24px;
  animation: rotate 2s linear infinite;
}

@keyframes rotate {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

.processing-title {
  font-size: 18px;
  font-weight: 500;
  color: var(--text-primary);
  margin-bottom: 8px;
}

.processing-subtitle {
  font-size: 14px;
  color: var(--text-muted);
  margin-bottom: 24px;
}

.processing-progress {
  height: 8px;
  background: var(--bg-tertiary);
  border-radius: 4px;
  overflow: hidden;
  margin-bottom: 16px;
}

.processing-progress-bar {
  height: 100%;
  background: var(--primary-purple);
  border-radius: 4px;
  transition: width 0.3s ease;
}

.processing-status {
  font-size: 13px;
  color: var(--text-secondary);
}
```

## エクスポート機能

### エクスポートボタン
```css
.export-button {
  padding: 8px 16px;
  background: transparent;
  border: 1px solid var(--border-color);
  border-radius: 8px;
  color: var(--text-primary);
  font-weight: 500;
  cursor: pointer;
  display: flex;
  align-items: center;
  gap: 8px;
  transition: all 0.2s ease;
}

.export-button:hover {
  background: var(--bg-hover);
  border-color: var(--primary-purple);
  color: var(--primary-purple);
}

/* エクスポートメニュー */
.export-menu {
  position: absolute;
  top: 100%;
  right: 0;
  margin-top: 8px;
  background: var(--bg-card);
  border: 1px solid var(--border-color);
  border-radius: 8px;
  box-shadow: var(--shadow-lg);
  min-width: 200px;
  overflow: hidden;
}

.export-menu-item {
  padding: 12px 16px;
  font-size: 14px;
  color: var(--text-primary);
  cursor: pointer;
  transition: background 0.2s ease;
  display: flex;
  align-items: center;
  gap: 12px;
}

.export-menu-item:hover {
  background: var(--bg-hover);
}

.export-menu-item-icon {
  color: var(--text-muted);
}
```